version: 2

models:
    - name: stg_transaction
      columns:
        - name: ta_id
          data_tests:
            - unique:
                config:
                    severity: warn
            - not_null:
                config:
                    severity: warn

    - name: stg_user
      columns:
        - name: ta_id
          data_tests:
            - unique:
                config:
                    severity: warn
            - not_null:
                config:
                    severity: warn
                